C = 0.7;
g = 9.81;
p = 1.112;
A = 0.025*pi;
m = 62.5;
x = 1;
v2 = zeros(1,4);
T2 = zeros(1,4);
v01 = zeros(1,601);
T01 = zeros(1,601);

for t = 2:2:6
    x = x + 1;
    T2(1,x) = t;
    v2(1,x) = v2(1,x-1) + (g - (C*p*A*v2(1,x-1)^2)/(2*m))*2;
end

for t = 0.01:0.01:6
    x = x + 1;
    T01(1,x) = t;
    v01(1,x) = v01(1,x-1) + (g - (C*p*A*v01(1,x-1)^2)/(2*m))*0.01;
end

plot(T2,v2,T01,v01)

Ea2 = (v01(1,201)-v2(1,2))/v01(1,201)*100
Ea4 = (v01(1,401)-v2(1,3))/v01(1,401)*100
Ea6 = (v01(1,601)-v2(1,4))/v01(1,601)*100